% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submission.R
\name{verify_submission}
\alias{verify_submission}
\title{Verify a downloaded submission's integrity hash}
\usage{
verify_submission(path)
}
\arguments{
\item{path}{Path to a JSON file downloaded via \code{\link[=download_answers_button]{download_answers_button()}}.}
}
\value{
Invisibly, a list with \code{ok} (logical) and the parsed submission
\code{content}. Also prints a human-readable summary.
}
\description{
Checks a JSON file downloaded via \code{\link[=download_answers_button]{download_answers_button()}} against its
embedded SHA-256 hash, to detect whether it was edited after being
downloaded (e.g. a wrong answer quietly changed to a right one before
being turned in). This is tamper-\emph{evidence}, not proof of identity: the
hash is computed entirely in the reader's own browser with no
server-held secret, so a technical reader could in principle reproduce
it themselves -- this is a deterrent and a check against casual editing,
not real cryptographic security.
}
\examples{
\dontrun{
verify_submission("class-101-2026-01-15T12-00-00-000Z.json")
}
}
