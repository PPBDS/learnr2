% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submission.R
\name{submission_time}
\alias{submission_time}
\title{Recover a submission's download time}
\usage{
submission_time(x)
}
\arguments{
\item{x}{Path to a JSON file downloaded via \code{\link[=download_answers_button]{download_answers_button()}},
or the raw \code{time} string from one.}
}
\value{
The download time as a \code{POSIXct} (UTC), invisibly. Also prints a
short summary --- with the reader's name, email, and device id when \code{x}
is a file.
}
\description{
\code{\link[=download_answers_button]{download_answers_button()}} writes the moment the reader clicked
"Download" into the JSON as a \code{time} field, lightly obfuscated: base-36 of
the epoch second run through a fixed multiply-and-offset. It is \emph{not}
encrypted --- the scheme is public, in \code{inst/extdata/quiz/quiz.js} ---
just enough that the raw timestamp isn't sitting in the file where a
student could read it, or swap in a different plausible time without
re-running the encoder. This function reverses it.
}
\details{
Nothing else in the file is hashed or signed, so this does \strong{not} detect
edited answers. If you need that, compare against work submitted through a
channel you control.
}
\examples{
\dontrun{
submission_time("class-101-answers.json")
}
# Decode a bare time code:
submission_time(learnr2:::encode_submission_time(as.POSIXct("2026-01-15 12:00:00", tz = "UTC")))
}
