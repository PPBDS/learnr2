% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/question.R
\name{question}
\alias{question}
\title{Create a quiz question}
\usage{
question(
  text,
  ...,
  type = c("auto", "single", "multiple", "text"),
  correct = "Correct!",
  incorrect = "Incorrect.",
  allow_retry = FALSE,
  random_answer_order = FALSE,
  submit_button = "Submit Answer",
  try_again_button = "Try Again",
  id = NULL
)
}
\arguments{
\item{text}{Question prompt.}

\item{...}{One or more \code{\link[=answer]{answer()}} objects.}

\item{type}{Question type. \code{"auto"} (the default) picks \code{"single"} when
exactly one answer is marked \code{correct}, and \code{"multiple"} otherwise. Use
\code{"text"} for a free-text question, where the reader types a response
that is compared (trimmed, whitespace-collapsed, case-insensitive)
against the \code{\link[=answer]{answer()}} text(s).}

\item{correct}{Message shown when the reader answers correctly.}

\item{incorrect}{Message shown when the reader answers incorrectly.}

\item{allow_retry}{Allow the reader to try again after an incorrect
answer? Defaults to \code{FALSE}.}

\item{random_answer_order}{Shuffle answer order each time the page loads?
Defaults to \code{FALSE}. Ignored for \code{type = "text"}.}

\item{submit_button, try_again_button}{Button labels.}

\item{id}{Stable identifier used to key the reader's saved answer (see
"Progress persistence" below). Defaults to a slug derived from \code{text}.
Set this explicitly if you plan to edit the question wording later and
want readers' saved answers to survive the edit.}
}
\value{
A \code{learnr2_question} object. Printed as an interactive HTML
widget, both in a rendered Quarto document and (via a browser preview)
at the R console.
}
\description{
A learnr-style quiz question, rendered as a small self-contained,
client-side interactive widget --- no Shiny or R server required. Supports
single-choice ("radio"), multiple-choice ("checkbox"), and free-text
questions, graded entirely in the reader's browser.
}
\section{Progress persistence}{

Once a reader submits an answer, it is saved in the browser's
\code{localStorage} (keyed by page URL and \code{id}) and restored on the next
visit. Because the default \code{id} is derived from \code{text}, editing a
question's wording changes its \code{id} and resets any saved answers for it;
pass \code{id} explicitly to avoid that.
}

\examples{
question(
  "What is 6 times 7?",
  answer("42", correct = TRUE),
  answer("36"),
  answer("48"),
  allow_retry = TRUE
)
}
