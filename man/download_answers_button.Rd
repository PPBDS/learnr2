% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submission.R
\name{download_answers_button}
\alias{download_answers_button}
\title{Add a "download my answers" button}
\usage{
download_answers_button(
  filename_prefix = "learnr2-answers",
  label = "Download My Answers"
)
}
\arguments{
\item{filename_prefix}{Prefix for the downloaded file's name. Defaults
to \code{"learnr2-answers"}.}

\item{label}{Button label. Defaults to \code{"Download My Answers"}.}
}
\value{
A \code{learnr2_download_button} object, printed as an interactive
HTML button.
}
\description{
Adds a button that, when clicked, gathers every \code{\link[=question]{question()}} and
\code{\link[=student_info]{student_info()}} answer currently on the page -- each already saved to
the browser's \code{localStorage} as the reader worked through the tutorial
-- into a single readable JSON file and downloads it. This happens
entirely in the reader's browser; there is no server to submit to, so
this is meant for a reader to save and turn in themselves (e.g. attach
to an email or upload to an LMS).
}
\examples{
download_answers_button()
}
