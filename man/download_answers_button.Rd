% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submission.R
\name{download_answers_button}
\alias{download_answers_button}
\title{Add a "download my answers" button}
\usage{
download_answers_button(
  filename_prefix = "learnr2-answers",
  label = "Download My Answers"
)
}
\arguments{
\item{filename_prefix}{Prefix for the downloaded file's name. Defaults
to \code{"learnr2-answers"}.}

\item{label}{Button label. Defaults to \code{"Download My Answers"}.}
}
\value{
A \code{learnr2_download_button} object, printed as an interactive
HTML button.
}
\description{
Adds a button that, when clicked, gathers every \code{\link[=question]{question()}} and
\code{\link[=student_info]{student_info()}} answer currently on the page -- each already saved to
the browser's \code{localStorage} as the reader worked through the tutorial
-- into a single readable JSON file and downloads it. This happens
entirely in the reader's browser; there is no server to submit to, so
this is meant for a reader to save and turn in themselves (e.g. attach
to an email or upload to an LMS).

The download's \code{exercises} array also includes every \verb{{webr}} code
exercise's current code, under the same condition quarto-live itself
requires to keep a record of it at all: \verb{#| persist: true} (already the
convention every bundled tutorial follows). An exercise without
\code{persist: true} has no saved copy of the reader's code anywhere --
\code{learnr2} included -- so it can't appear in the download; this is a
structural limit of quarto-live's own editor, not something
\code{download_answers_button()} chooses to skip.
}
\details{
The download includes a \code{metadata} block (timestamp, timezone, browser
info, and a random per-device id persisted across the reader's visits)
and a SHA-256 \code{integrity} hash over the content. Check a downloaded file
with \code{\link[=verify_submission]{verify_submission()}}. This is tamper-\emph{evidence}, not proof of
identity: since everything runs in the reader's own browser with no
server-held secret, a technical reader could reproduce the hash
themselves. What it reliably catches is editing the file afterward (e.g.
changing a wrong answer to a right one before turning it in).
}
\examples{
download_answers_button()
}
