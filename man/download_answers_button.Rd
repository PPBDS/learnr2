% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submission.R
\name{download_answers_button}
\alias{download_answers_button}
\title{Add a "download my answers" button}
\usage{
download_answers_button(
  filename_prefix = "learnr2-answers",
  label = "Download My Answers"
)
}
\arguments{
\item{filename_prefix}{Prefix for the downloaded file's name. Defaults
to \code{"learnr2-answers"}.}

\item{label}{Button label. Defaults to \code{"Download My Answers"}.}
}
\value{
A \code{learnr2_download_button} object, printed as an interactive
HTML button.
}
\description{
Adds a button that, when clicked, gathers every \code{\link[=question]{question()}} and
\code{\link[=student_info]{student_info()}} answer currently on the page -- each already saved to
the browser's \code{localStorage} as the reader worked through the tutorial
-- into a single readable JSON file and downloads it. This happens
entirely in the reader's browser; there is no server to submit to, so
this is meant for a reader to save and turn in themselves (e.g. attach
to an email or upload to an LMS).
}
\details{
The download's \code{answers} array is one flat list: every \code{\link[=question]{question()}} plus
every \code{{webr}} code exercise, each as a \verb{\{ id, answer \}} pair keyed by
the widget's (or exercise's) stable id. A question never submitted has
\code{answer: null}; a choice question's \code{answer} is an array of the picked
options; an image-paste reflection's \code{answer} is the pasted screenshot
as a PNG data-URL string.

A \code{{webr}} exercise appears only under the condition quarto-live itself
requires to keep a record of the reader's code at all: \verb{#| persist: true} (already the convention every bundled tutorial follows). An
exercise without \code{persist: true} has no saved copy of the reader's code
anywhere -- \code{learnr2} included -- so it can't appear in the download;
this is a structural limit of quarto-live's own editor, not something
\code{download_answers_button()} chooses to skip.

The download also carries a \code{metadata} block (timezone, browser info, and
a random per-device id persisted across the reader's visits) and a \code{time}
field: the moment the reader clicked "Download", lightly obfuscated so the
raw timestamp isn't readable or hand-editable in the file. Recover it with
\code{\link[=submission_time]{submission_time()}}. Nothing else is hashed or signed -- a determined
reader can still edit their answers; this only keeps the submission time
honest at a glance.
}
\examples{
download_answers_button()
}
