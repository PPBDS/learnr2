---
title: "{{title}}"
format: live-html
engine: knitr
toc: true
webr:
  packages:
    - dplyr
---

{{< include _extensions/r-wasm/live/_knitr.qmd >}}

## Student Information

```{r}
learnr2::student_info()
```

## Welcome

Write your tutorial here. The chunks below run entirely in the reader's
browser using WebR --- no R server required.

## A runnable cell

```{webr}
x <- c(1, 2, 3, 4, 5)
mean(x)
```

## An exercise

```{webr}
#| exercise: first_exercise
#| persist: true
# Compute the sum of 1 through 10.
______
```

::: { .hint exercise="first_exercise" }
The `sum()` function adds its arguments. Try `sum(1:10)`.
:::

::: { .solution exercise="first_exercise" }
```r
sum(1:10)
```
:::

## A quiz question

```{r}
learnr2::question(
  "What does `sum(1:10)` return?",
  learnr2::answer("55", correct = TRUE),
  learnr2::answer("50"),
  learnr2::answer("100"),
  allow_retry = TRUE
)
```
