{
  "name": "learnr2-js-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright tests for the quiz.js runtime bundled in inst/extdata/quiz.",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --config=playwright.smoke.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
