1) How do I start a tutorial? The old way is this:
learnr::run_tutorial(tutorial = "hello", package = "learnr")

    learnr2::run_tutorial('hello-learnr2', open = TRUE)


4) You need to understand the tests. Is there really a test for the copy/paste the image?

    Now yes: tests/js/quiz.spec.js, run via Playwright (real browser, real
    clipboard, real Ctrl+V), wired into CI at .github/workflows/js-tests.yaml.
    Before this it was only manually checked by hand, never automated --
    testthat can't test browser JS at all.

5) How do we find all the installed tutorials, e.g. for VS Code extension?

6) Student information and download answers defaults. With tutorial.helpers, we have:
```{r info-section, child = system.file("child_documents/info_section.Rmd", package = "tutorial.helpers")}
```

```{r download-answers, child = system.file("child_documents/download_answers.Rmd", package = "tutorial.helpers")}
```

7) Why template? Why extdata? What do they do? (Text Answer)


2) Can we have the example tutorial appear on-line (maybe GitHub Pages) and allow students to do it there, including downloading the answers?
    8) Check out GH Pages webhosting