0) Have AI make a new repo (in your personal GitHub) which is vscode.tutorials2 (start by forking vscode.tutorials), for learnr2. Create a CLAUDE.md which guides Claude in how to take a learnr tutorial and turn it into a learnr2 tutorial. Do it.


1) Learn about devtools::check() and R CMD check in general. And how the .github YAML files work, especially R CMD check.

2) What happens to the work we have done when we quit the browser? Can we re-open the tutorial and have all our work saved? What happens if we restart our computer?

3) Tutorials must be discoverable from R Tutorials VS Code Extension. See https://github.com/PPBDS/vscode-r-tutorials. My preference is that we don't change this extension. Instead, I think that we need functions in learnr2 which are the equivalent of useful functions in learnr. Do not edit the extension. Fixing it is the last thing we do. This is just a note for the future.

4) how do we make it super easy for authors (or AIs!) to add our three standard intro questions and the download question about minutes and the down answers button?


6) Student information and download answers defaults. With tutorial.helpers, we have:
```{r info-section, child = system.file("child_documents/info_section.Rmd", package = "tutorial.helpers")}
```

```{r download-answers, child = system.file("child_documents/download_answers.Rmd", package = "tutorial.helpers")}
```

 
7) Why template? Why extdata? What do they do? (Text Answer)


2) Can we have the example tutorial appear on-line (maybe GitHub Pages) and allow students to do it there, including downloading the answers?
    8) Check out GH Pages webhosting

